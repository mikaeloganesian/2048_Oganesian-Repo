using UnityEngine;

public class CellView : MonoBehaviour
{
    //Не успел вынести логику из GameField :(
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
